# Smart Calculator

## Development

* build images:
```
docker-compose build --pull api
```

* run linters:
```
docker-compose run api lint
```

* run tests:
```
docker-compose run api test
```

* run formatters:
```
docker-compose run api lint_fix
```
